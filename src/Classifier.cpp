#include "Classifier.hpp"

using namespace sonar_image_feature_extractor;

bool Classifier::classify(Cluster c, SVMConfig config){
  
 
  return false;
}

bool Classifier::learn( std::vector<Cluster> positives, std::vector<Cluster> negatives, SVMConfig config){
  
  
  return false;
}